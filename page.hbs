{{!< default}}
<article class="container {{post_class}}" id="post-{{id}}">
	{{#post}}
		<header class="c-10 off-01 post-header">
			<h1>{{{title}}}</h1>
		</header>
		<section class="c-10 off-01 post-content">
			{{content}}
		</section>
	{{/post}}
</article>